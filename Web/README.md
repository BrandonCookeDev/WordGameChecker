# Word Game Checker Program
## SETUP
- Clone Repository
- Download NodeJS
- In project directory run ```npm install```. This will install all node dependencies
- In the client directory, run ```bower install```. This will install all bower frontend dependencies

